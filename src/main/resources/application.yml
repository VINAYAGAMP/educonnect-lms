spring:
  datasource:
    url: jdbc:mysql://localhost:3306/educonnect?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Soloheart@09092003 # Verify this password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  batch:
    jdbc:
      initialize-schema: never   # Fixed: Prevents unnecessary initialization errors
    job:
      enabled: false

server:
  port: 8084 # Using 8084 to avoid conflicts

logging:
  level:
    com.educonnect: DEBUG
    org.hibernate.SQL: DEBUG